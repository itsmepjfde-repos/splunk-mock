[project]
name = "splunk-mock"
version = "0.2.0"
requires-python = ">=3.11"
dependencies = [
	"fastapi>=0.115",
	"pydantic>=2.0",
	"uvicorn[standard]>=0.30",
]

[tool.vercel]
entrypoint = "api.splunk_mock:app"
